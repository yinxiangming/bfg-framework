export const metadata = { title: 'Settings - Delivery - Edit Freight Service' }

export default function EditFreightServiceLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
