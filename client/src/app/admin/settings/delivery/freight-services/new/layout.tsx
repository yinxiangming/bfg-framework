export const metadata = { title: 'Settings - Delivery - New Freight Service' }

export default function NewFreightServiceLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
