// Segment layout for account root (dashboard) - metadata only
export const metadata = { title: 'Dashboard' }

export default function AccountDashboardLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
