# Public site origin for this marketing app (no trailing slash). Required for absolute URLs in metadata/sitemaps if you add them later.
NEXT_PUBLIC_SITE_URL=

# BFG / main app origin for cross-links (e.g. Sign in). No trailing slash.
NEXT_PUBLIC_MAIN_APP_ORIGIN=
